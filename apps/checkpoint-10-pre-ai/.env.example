# OpenAI API key for Casper (the AI concierge). Required for /api/chat.
OPENAI_API_KEY=sk-...

# Optional: override the chat model (default: gpt-4o)
# OPENAI_MODEL=gpt-4o-mini

# Optional: where the fake Ghost Airlines API runs (default: http://localhost:3300)
# VITE_API_URL=http://localhost:3300
