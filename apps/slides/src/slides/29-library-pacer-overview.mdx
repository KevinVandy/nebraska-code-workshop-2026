<LibraryOverviewSlide library="pacer">

- I made this one!
- 5 timing patterns: **debouncing, throttling, rate limiting, queuing, batching**
- Sync and async variants with promises, error handling, retry, and abort
- Fixed or sliding rate-limit windows;
- FIFO/LIFO/priority queues with concurrency limits
- Three API layers: core classes, functions, or React hooks
- Opt-in reactive state via selectors — only re-render for what you track

```tsx
import { useDebouncer } from "@tanstack/react-pacer"

function Search() {
  const debouncer = useDebouncer((q: string) => fetchResults(q), { wait: 500 })
  return <input onChange={(e) => debouncer.maybeExecute(e.target.value)} />
}
```

</LibraryOverviewSlide>
