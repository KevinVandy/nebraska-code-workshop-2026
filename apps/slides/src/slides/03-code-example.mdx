## Slides are just MDX

Markdown, code blocks, and even React components:

```tsx
import { useHotkey } from '@tanstack/react-hotkeys'

useHotkey('ArrowRight', () => goTo(index + 1))
useHotkey('ArrowLeft', () => goTo(index - 1))
```

This deck itself uses **TanStack Hotkeys** for navigation.
